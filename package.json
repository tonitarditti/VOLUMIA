{
  "name": "volumia",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm --workspace @volumia/desktop run dev",
    "build": "npm --workspace @volumia/desktop run build",
    "lint": "npm --workspace @volumia/desktop run lint",
    "typecheck": "npm --workspace @volumia/desktop run typecheck",
    "test": "npm --workspace @volumia/desktop run test",
    "format": "prettier --write \"apps/**/*.{ts,tsx,css,json,md,py}\" \"packages/**/*.{ts,json,md}\"",
    "clean": "node scripts/clean.js",
    "clean:deep": "node scripts/clean.js --deep"
  },
  "overrides": {
    "@types/react": "18.3.28",
    "@types/react-dom": "18.3.7"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
